/** @type {import('next').NextConfig} */
const nextConfig = {
  // The dev overlay badge sits over the sidebar's sign-out button; off for demo runs.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
